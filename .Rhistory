library(googledrive)
cache.path <- file.path(".", "Data")
stage.path <- file.path("~/Data", "employee-surveys")
drive_auth()
# Show files
drive_ls(stage.path)
records.file <- "Employee records.csv"
file.path(cache.path, records.file)
records.cache <- file.path(cache.path, records.file)
records.source <- file.path(stage.path, records.file)
records.source
### DL
records.id <- records.source %>% drive_get() %>% as_id()
records.id
drive_download(records.id, records.cache, overwrite = TRUE)
